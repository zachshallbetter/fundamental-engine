/**
 * `@forces-ui/vanilla` — deprecated compatibility alias of `@field-ui/vanilla`.
 *
 * Renamed in the field-ui migration. This shim re-exports `@field-ui/vanilla` unchanged so
 * existing imports keep working. Prefer `@field-ui/vanilla` in new code; this alias is slated
 * for removal in a future major (see `docs/planning-archive/field-ui-migration-plan.md`).
 */
export * from '@field-ui/vanilla';
