export default defineNuxtConfig({
  // Static generation: `nuxt generate` → .output/public/
  // Swap ssr: false → ssr: true for a real SSR/Node server deployment.
  ssr: false,
  devtools: { enabled: false },
});
