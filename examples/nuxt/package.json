{
  "name": "fundamental-example-nuxt",
  "private": true,
  "version": "0.0.0",
  "description": "Nuxt 3 + @fundamental-engine/vanilla. The engine is loaded in onMounted() — never runs during SSR.",
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt generate",
    "preview": "nuxt preview"
  },
  "dependencies": {
    "@fundamental-engine/vanilla": "^0.9.0",
    "nuxt": "^3"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
