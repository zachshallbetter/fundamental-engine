{
  "name": "fundamental-example-nextjs",
  "private": true,
  "version": "0.0.0",
  "description": "Next.js (App Router) + @fundamental-engine/react. The field runs in a 'use client' component loaded dynamically (ssr:false) so SSR never touches the engine.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "@fundamental-engine/react": "^0.9.0",
    "next": "^15",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "typescript": "^5"
  }
}
