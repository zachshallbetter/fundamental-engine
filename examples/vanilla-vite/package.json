{
  "name": "fundamental-example-vanilla-vite",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone Vite + TypeScript example: createField() on a <canvas> you own, with [data-body] elements the field reacts to. Depends on the published @fundamental-engine/vanilla.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@fundamental-engine/vanilla": "^0.9.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^6"
  }
}
