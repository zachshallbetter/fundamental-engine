node_modules
dist
coverage
*.log
.DS_Store
.idea
.vscode
*.tsbuildinfo

# Apple signing material — NEVER commit private keys / certs / profiles
*.p12
*.cer
*.p8
*.mobileprovision
*.keychain-db
DeveloperID*

# Astro generated cache
.astro/
.vercel/

# env (defensive — none are used, but never commit secrets)
.env
.env.*
!.env.example

.cursorrules
.aider*
.github/copilot-instructions.md

# Playwright artifacts (apps/site e2e)
test-results/
playwright-report/
.board_snapshot.*

# Swift Package Manager & Xcode build artifacts / local settings
swift/.build/
swift/build/
swift/.swiftpm/xcode/
*.xcuserdata/
*.xcuserstate

# Turborepo
.turbo/

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

.graphifyignore
graphify-out

# Research paper LaTeX/PDF build output (`pnpm gen:papers`)
docs/research/build/

# Generated agent corpus (regenerated by gen-llms.mjs during build; not tracked to avoid churn)
apps/site/public/llms.txt
apps/site/public/llms-full.txt
