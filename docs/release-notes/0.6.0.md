# 0.6.0

> Shipped 2026-06-15 — YANKED.

A burned release, recorded for completeness. `0.6.0` was published to npm on 2026-06-15
(`release: v0.6.0`, #483), then rolled back the **same cycle** — it was cut prematurely (a thin batch
with one half-finished feature), and the repository was reverted to 0.5.0 (`revert: roll the repo back to
0.5.0`, #484). The npm version can never be republished, so the published line skips from 0.5.1 to 0.7.0.

> **Do not install `@fundamental-engine/*@0.6.0`.** This version has no GitHub release; the entry exists
> so every version that ever reached the registry is accounted for.

## Yanked

- Published to npm then rolled back the same day; superseded by 0.7.0. The substantive work it contained
  was re-shipped, complete, in later releases. See the CHANGELOG `[0.6.0]` entry.
