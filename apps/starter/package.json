{
  "name": "@fundamental-engine/starter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The first five minutes with Fundamental — a plain Vite + HTML app that consumes the packages exactly as an outside project would: declarative <field-root>, programmatic applyRecipe(), data-driven bindData(), a reduced-motion fallback, and a live inspector readout.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fundamental-engine/elements": "workspace:*",
    "@fundamental-engine/platform": "workspace:*",
    "@fundamental-engine/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^6"
  }
}
