{
  "name": "@field-ui/starter",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The first five minutes with field-ui — a plain Vite + HTML app that consumes the packages exactly as an outside project would: declarative <field-root>, programmatic applyRecipe(), data-driven bindData(), a reduced-motion fallback, and a live inspector readout.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@field-ui/elements": "workspace:*",
    "@field-ui/platform": "workspace:*",
    "field-ui": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^6"
  }
}
